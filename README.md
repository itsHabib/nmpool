# nmpool

A local, private dependency-install cache for macOS and Windows.

Initial implementation is being developed on an isolated branch. Cache entries are
platform-specific. Existing installs are never adopted, linked, replaced or deleted.
