# Validation

Initial local checks: formatting, strict Clippy, contract tests and CLI smoke.
Native Windows and macOS CI and an isolated personal workload are being run for the
initial PR. Exact results will be recorded here before handoff.

No live install has been adopted or overwritten. No garbage collection or merge is
part of this task. Actual Windows-laptop performance remains unmeasured.
